# 适航审查智能体 · 14 周开发路线图

> 原则：**每阶段独立产出可演示的 MVP**，第 6 周末即可写简历投递，之后持续更新反而体现成长性。
> 每周结束在 GitHub 提交一次——绿色格子本身就是简历。
> 时间不够时按文末「降级顺序」收缩，但**五项底线永不动刀**：意图路由、SQL 安全校验、自纠错环、步骤追踪、条款引用溯源。

---

## 第 0 阶段（第 1-2 周）：基础与 LLM API 入门

**目标**：跑通第一个 LLM 应用，掌握全部后续工作的基础设施。

### 第 1 周
- [ ] 环境：Python 3.10+、VS Code、Git；学会 venv 创建/激活；GitHub 建仓 `airworthiness-review-agent`
- [ ] 注册 DeepSeek 开放平台，创建 API Key（充值 10 元够用数月）
- [ ] Python 语法补课（只学三个主题）：函数、字典/列表、类
- [ ] **手写裸调用**：用 httpx 直接 POST DeepSeek 接口（自己拼 JSON、解析流式响应）——彻底理解 OpenAI 兼容协议，面试素材
- [ ] 用 openai SDK 重写同一调用（SDK 只是协议客户端，与 OpenAI 服务零关系）

### 第 2 周
- [ ] 命令行问答机器人（src/phase0/chat_cli.py）：多轮对话 + system prompt 设定为「适航工程顾问」
- [ ] 学习概念：temperature、messages 列表、多轮对话原理
- [ ] Prompt 实验：同一问题 3 种问法对比回答，记入 notebooks/
- [ ] **领域认知**：通读 CCAR-21 目录结构，建立「规章(CCAR) - 程序文件(AP) - 咨询通告(AC)」文件体系概念

**✅ 验收**：终端能和带角色的机器人多轮对话；能解释 messages 结构；能说清规章/AP/AC 三层文件体系。

---

## 第 1 阶段（第 3-6 周）：多规章 RAG + 知识库管理 ← MVP①（第一个简历亮点）

**目标**：规章问题进，带条款号的溯源回答出；支持上传文档入库。

### 第 3 周：数据与库表
- [ ] 运行下载脚本拉取 FAR Part 23/25（eCFR API，XML 全自动）
- [ ] 手动下载 CCAR-25-R4（Word 版）、CCAR-23-R4 放入 data/raw/
- [ ] 学习规章文档结构：部(Part) → 分部(Subpart) → 条(§) → 款(a/b/c) → 项(1/2/3)
- [ ] SQLite 库表设计：documents（文档表）+ clauses（条款表，含 部/分部/条/款 层级元数据、语言、owner_id）
- [ ] 文档解析器：docx（python-docx）、XML（lxml）、PDF（pdfplumber）三种格式

### 第 4 周：切分与检索
- [ ] 条款级切分器（scripts/chunk_regulations.py）：按「条」为主单位，长条按「款」细分；CCAR 中文正则 + eCFR XML 结构解析双通道
- [ ] 向量化入库流水线：bge-m3 本地 Embedding → sqlite-vec 向量列 + FTS5 全文索引同库写入；增量入库、删除级联
- [ ] 混合检索：FTS5(BM25) 与向量余弦各取 top-10 → RRF 融合 → bge-reranker 重排 → top-3
- [ ] notebook 实验 10 个问题：跨语言检索效果（中文问题查英文 FAR 条款）

### 第 5 周：生成与体验
- [ ] RAG prompt：强制「只依据提供的条款回答 + 标注条款号（如 CCAR-25.813(b)）」
- [ ] 拒答机制：检索不到相关条款时明确回答「知识库未找到依据」
- [ ] 多轮追问：处理「那它的(b)款呢？」类指代
- [ ] 封装为 src/rag/pipeline.py

### 第 6 周：评估 ← 面试必问「你怎么衡量效果」
- [ ] 自建 40-50 条评测集（data/eval/questions.jsonl：中英混合问题 + 标准条款号）
- [ ] 指标：Recall@5（检索命中率）、引用条款正确率、幻觉率
- [ ] 对比实验表：纯向量 vs 纯 BM25 vs 混合+重排（放 README，硬通货）

**✅ 验收**：问「CCAR-25 对应急出口有什么要求」返回带条款号的溯源回答；评测报告完成。**此刻起可投简历**。

---

## 第 2 阶段（第 7-9 周）：适航知识工具 + MCP ← MVP②（第二个简历亮点）

**目标**：Agent 获得确定性工具，「算得出、查得准、比得了」。

### 第 7 周：领域知识结构化
- [ ] MC0–MC9 方法定义库（依据 AP-21 审定程序）：MC0 符合性声明 / MC1 说明性文件 / MC2 分析计算 / MC3 安全评估 / MC4 实验室试验 / MC5 地面试验 / MC6 飞行试验 / MC7 航空器检查 / MC8 模拟器试验 / MC9 设备鉴定
- [ ] 适用性规则表：CCAR-23-R4 新分级（4 审定等级 × 2 性能等级）、CCAR-25 运输类、CCAR-92 无人机
- [ ] 条款类型启发式规则（如「飞行试验类要求倾向 MC6」），作为 moc_suggest 的依据之一

### 第 8 周：工具调用
- [ ] 四个工具注册为 Function Calling：reg_lookup / reg_search / reg_diff / moc_suggest
- [ ] 两步调用循环：LLM 决定调工具 → 拿结果 → 继续推理；调用链路可视化
- [ ] 演示用例：「对比 CCAR 25.981 与 FAR 25.981」全流程自动完成
- [ ] **穿插学习（本周起）**：2-3 天 JS 语法速成（对照 Python 概念迁移）

### 第 9 周：MCP 封装（2025-2026 热点加分项）
- [ ] 四工具封装为 MCP Server（官方 Python SDK）
- [ ] 演示：任意支持 MCP 的客户端连接使用你的适航工具——「行业垂直 MCP 服务器」
- [ ] 每周 3-4 小时 Vue 3 官方中文教程（跑通 + Element Plus 拼一个静态页）

**✅ 验收**：工具调用链路可视化；MCP Server 可被外部客户端连接调用。

---

## 第 3 阶段（第 10-14 周）：Agent 引擎 + 前后端 + 多用户 ← MVP③（第三个简历亮点）

**目标**：完整产品形态 + 对外发布。

### 第 10 周：LangGraph 编排
- [ ] 学习 StateGraph、节点、条件边、checkpointer、interrupt
- [ ] 意图识别四路路由：RAG 语义类 / SQL 统计类 / reg_diff 对比类 / 矩阵任务类
- [ ] 分析计划生成（plan-and-execute，计划先展示再执行）+ 步骤追踪事件流
- [ ] FastAPI 服务层骨架：REST + SSE 流式接口（自动 OpenAPI 文档）

### 第 11 周：Text-to-SQL 链路 + Vue 启动
- [ ] sql_query 工具：基于 documents/clauses/矩阵/会话表 schema 生成 SELECT
- [ ] SQL 安全校验（sqlglot 语法树级）：仅 SELECT、表白名单、强制 LIMIT、禁多语句
- [ ] 自纠错环：报错信息 + schema 回喂重写 SQL，上限 3 次
- [ ] 结果摘要压缩 + AI 数据解读（关联条款引用）
- [ ] Vue：登录页 + 对话页（流式输出、Agent 步骤时间线、引用展开）

### 第 12 周：工作台与联调
- [ ] 符合性矩阵工作流接入：产品描述 → 类别判定 → 批量初筛（适用/不适用/需专题研究）→ MOC 建议 → interrupt 人工确认
- [ ] Vue：矩阵工作台页（逐条/批量确认）+ 知识库管理页（上传/文档列表/重建索引）+ 报告页（ECharts 图表）
- [ ] 矩阵导出：Excel（openpyxl）/ Markdown；报告导出 Markdown
- [ ] 前后端联调，跑通 3 个演示场景：正常类飞机（CCAR-23）/ 运输类（CCAR-25）/ 大型无人机（CCAR-92，选做）

### 第 13 周：多用户
- [ ] JWT 鉴权（注册/登录，passlib 加密）；users 表 + role 字段
- [ ] RBAC：admin（全局知识库管理、用户管理、全量审计视角）/ user（问答、个人空间、自己的矩阵）
- [ ] 数据隔离改造：检索过滤 `WHERE owner_id IS NULL OR owner_id=当前用户`；会话/矩阵/个人文档按 user_id 隔离
- [ ] admin 种子脚本（管理员不开放网页注册）；前端按角色渲染菜单

### 第 14 周：打磨发布
- [ ] 部署：Vite 打包由 FastAPI 托管静态文件，单服务上线（Streamlit Cloud 不适用，选 Render / HF Space）；Docker 打包选做
- [ ] README 完善：架构图、评测数据表、录屏 GIF、快速开始
- [ ] 2 篇技术博客：「适航规章条款级 RAG 实践」「多 Agent 辅助生成符合性矩阵」
- [ ] 简历提炼 3 条要点（每阶段一条，含量化指标）

**✅ 验收**：陌生人按 README 能跑起来；3 分钟演示视频讲清四层能力；简历可写。

---

## 里程碑总览

| 时间 | 交付物 | 简历可写 |
|------|--------|---------|
| 第 2 周末 | 命令行问答机器人 | （练手，不写） |
| 第 6 周末 | MVP①：多规章 RAG 问答 + 评测报告 | 「构建条款级混合检索 RAG 系统，Recall@5 较单路提升 X%」 |
| 第 9 周末 | MVP②：适航知识工具 + MCP Server | 「封装 4 个领域工具为 MCP 服务，可被任意 AI 客户端调用」 |
| 第 14 周末 | MVP③：完整多用户系统 + 演示视频 | 「基于 LangGraph 建模符合性审查工作流（意图路由/Text-to-SQL/自纠错/人工确认）」 |

## 降级顺序与底线

**进度告急时依次砍**：个人文档空间 → 报告图表 → 推荐追问 → 多用户降级为访客会话模式。

**永不动刀的五项底线**（技术灵魂）：意图路由、SQL 安全校验、自纠错环、步骤追踪、条款引用溯源。

## 风险与对策

- **MOC 建议的权威性** → 定位「辅助建议 + 工程师确认」，human-in-the-loop 是设计立场而非妥协
- **政府站点反爬** → 中文规章手动下载（总量 6-8 个文件）；FAR/EASA 全自动
- **跨语言检索效果不确定** → bge-m3 多语言 + 第 4 周预留调参；兜底方案中英分索引 + 查询路由
- **Vue 学习曲线** → wk8 前置 JS 速成 + Element Plus 组件化开发；界面糙一点不影响作品价值
- **求职提前到来** → 第 6 周末 MVP① 即可投递，作品持续更新反而加分
